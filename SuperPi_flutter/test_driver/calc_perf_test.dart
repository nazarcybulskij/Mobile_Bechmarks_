import 'package:SuperPi/common.dart';

import 'util.dart';

void main() {

  macroPerfTest(
    'super_calc',
    '/asd',
    pageDelay: const Duration(seconds: 1),
    duration: const Duration(seconds: 5),
  );
}